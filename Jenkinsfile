pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
        stage('build2') {
            steps {
                sh 'python --version'
            }
        }
    }
}
